import { FormControl, FormLabel } from '@chakra-ui/react';
import styled from 'styled-components';

export const FormBlock = styled(FormControl)`
  margin-bottom: 16px;
`;

export const Label = styled(FormLabel)`
  font-weight: 700 !important;
`;
